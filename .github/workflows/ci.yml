<<<<<<< HEAD
name: CI

on:
  push:
    branches:
      - main


jobs:
  docker:
    runs-on: ubuntu-latest


    steps:

      - name: checkout
        uses: actions/checkout@v4

      - name: login docker
        uses: docker/login-action@v3
        with:
          username: ${{secrets.DOCKER_UERNAME}}
          password: ${{secrets.DOKCER_PASSWORD}}

        
      - name: build docker
        run: |
          docker build -t ${{secrets.DOCKER_USERNAME}}/docker-ci-demo:latest .
      
      - name: push dokcer 
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/docker-ci-demo:latest

          
=======
name: node ci-cd
on:
  push:
    branches:
     - master

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - run: npm install

      - run: npm test
>>>>>>> 722291ce899b3479b7f20cfba4a2cd4728a49b19
